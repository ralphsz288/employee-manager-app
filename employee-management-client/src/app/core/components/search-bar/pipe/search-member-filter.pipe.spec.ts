import { SearchMemberFilterPipe } from './search-member-filter.pipe';

describe('SearchMemberFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchMemberFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
